## 1. Implementation

- [ ] 1.1 Fix `snapshot.data!` in `lib/main.dart` (AppInitializer).
- [ ] 1.2 Fix `snapshot.data!` in `lib/ui/widgets/org_state_provider.dart`.
- [ ] 1.3 Fix `snapshot.data!` in `lib/ui/screens/org_settings/org_settings_screen.dart`.
- [ ] 1.4 Add null checks to `AuthStateChangeAction` in `lib/auth.dart`.

## 2. Validation

- [ ] 2.1 Run `flutter analyze` to ensure static analysis passes.
- [ ] 2.2 Run `flutter test` to ensure no regressions.
