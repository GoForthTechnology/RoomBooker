## 1. UI Updates

- [ ] 1.1 Update `ViewBookingsScreen` to conditionally render the `FloatingActionButton` only when `!viewState.showEditor`.

## 2. Verification

- [ ] 2.1 Manually verify or run tests to ensure the FAB is hidden when the request editor panel is active.
- [ ] 2.2 Manually verify or run tests to ensure the FAB is visible when the calendar is not in Day view and the request editor panel is closed.
